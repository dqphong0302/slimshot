{
  "name": "slimshot",
  "version": "1.0.0",
  "description": "SlimShot - Fast, privacy-first batch image compressor and format converter. 100% Client-side in browser RAM.",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "vite build && wrangler deploy"
  },
  "devDependencies": {
    "vite": "^8.2.2",
    "wrangler": "^4.127.1"
  },
  "dependencies": {
    "jszip": "^3.10.1"
  }
}
